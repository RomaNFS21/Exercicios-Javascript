// Declaração do vetor
var numbers = [10, 20, 30, 40, 50]

// Adiciona o valor 60 ao final do vetor
numbers.push(60)
console.log(numbers)

// Retira o ultimo valor do vetor
numbers.pop()
console.log(numbers)

// Mostra a posição do valor 30
console.log(numbers.indexOf(30))